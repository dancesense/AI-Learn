spring:
  application:
    name: werewolf-assistant
  datasource:
    url: jdbc:mysql://localhost:3306/werewolf_assistant?useUnicode=true&characterEncoding=utf8&serverTimezone=Asia/Shanghai
    username: ${MYSQL_USER:root}
    password: ${MYSQL_PASSWORD:root}
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    open-in-view: false
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
  ai:
    dashscope:
      # 临时硬编码（仅用于本地排障），后续建议改回环境变量
      api-key: sk-79729ca14be743e7a33ba6591c788b07
      chat:
        api-key: sk-79729ca14be743e7a33ba6591c788b07

server:
  port: 8012

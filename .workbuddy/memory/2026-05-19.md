# 2026-05-19 工作日志

## 狼人杀助手 — 7项UI/交互优化

### 1. 录音按钮增大（#11）
- `.pcard-mic` 从 20x20 → 36x36px，圆角从999px→10px
- 录音中增加脉冲动画 `mic-pulse`，更醒目
- 图标从 🎙→🎤，录音中显示 ●
- 边框改为红色调 `rgba(255,80,113,0.5)`

### 2. 玩家编号标红 + 卡片简化（#12）
- `highlightKeywords` 增加"X号"正则匹配 → `.kw-player` 红色高亮
- 卡片body只显示最后一段话的前40字（原60字）
- 完整对话仍需点眼睛图标查看

### 3. 触屏操作优化（#13）
- 卡片增加 `touch-action: manipulation` + `-webkit-tap-highlight-color: transparent`
- 卡片active增加 `transform: scale(0.97)` 反馈
- 按钮 `.vote-add-btn` padding从 `0 12px`→`8px 14px`，min-height 36px
- 状态标签padding增大 `1px 5px`→`3px 7px`
- 投票选择器高度 32→36px
- 眼睛按钮 22→28px

### 4. AI话术提示词优化（#14）
- speechAdvice prompt 从"资深狼人杀教练"改为"普通狼人杀玩家"
- 要求话术像真人说话：口语、有停顿犹豫、不要太流畅完美
- 禁止使用"综上所述""由此可见""逻辑闭环"等书面词汇
- 适当留有破绽和模糊空间
- 信息不够时允许说"还没看明白"

### 5. 点击新玩家录音自动结束上一个（#15）
- `onPlayerMicClick` 中切换玩家时，先完全停止 `running=false` + `stopMediaRecorder` + `isTranscribing=false`

### 6. 删除神职技能记录区块（#16）
- HTML: 删除 skillPanel 整个section（预言家验/女巫用药/守卫守护）
- HTML: 删除 .skill-row/.skill-label/.skill-select/.skill-btn CSS
- JS: 删除 prophetTarget/prophetResult/prophetBtn/witchTarget/witchAction/witchBtn/guardTarget/guardBtn/skillLogList DOM引用
- JS: 删除 renderSkillSelects/renderSkillLogs/addSkillLog 函数
- JS: 删除三个技能按钮的click事件绑定
- JS: renderVoteSelects 不再包含技能选择器

### 7. 录音稳定性优化（#17）
- `startMediaRecorder` 增加 timeslice=3000（每3秒产生一次数据，避免长录音丢失）
- `mediaRecorder.start(timeslice)` 替代 `mediaRecorder.start()`
- FileReader 增加 onerror 回调
- onerror 后延迟500ms重启（避免频繁出错）
- `initAudioResources` 增加麦克风权限错误提示
- 增加 echoCancellation/noiseSuppression/autoGainControl
- `startRecordingForPlayer` 增加 AudioContext.resume() 逻辑（iOS Safari兼容）
- resourcesReady 检查：音频未就绪时不开始录音

### 编译结果
- `mvn compile -q` 成功
